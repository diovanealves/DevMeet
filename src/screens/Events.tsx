import { View, Text } from 'react-native'
import { NavigationProps } from '../@types/navigation'

export default function Events({ navigation }: NavigationProps<'Events'>) {
  return (
    <View>
      <Text>Events</Text>
    </View>
  )
}
